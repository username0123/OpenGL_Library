package org.grgodgames.lib;

import java.util.logging.Logger;

/**
 * The type Buffer helper.
 *
 * @author Greg Brown (GrGod123)
 * @version 1.0.0
 * @since CORE
 */
@Utility
final class BufferHelper {
	private static final GameLogger LOGGER = GameLogger.loggerOf(Logger.getLogger(BufferHelper.class.getName()));

	/** Instantiates a new Buffer helper. */
	private BufferHelper() {
	}
}
